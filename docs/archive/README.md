# Archive — retired docs

> type: index · status: deprecated · updated: 2026-07-13

**[deprecated]** Everything under `docs/archive/` documents the **retired old stack**: the review-CSV
data source, the FE tag-embedding pipeline, and the LangGraph/Streamlit "vibe" recommender. That
stack lost to an LLM-with-library baseline on its anonymous/vibe framing and was replaced by the
behavioral CF stack. Kept as history — do not treat any page here as current.

Canonical, current docs: [`../README.md`](../README.md). Why the pivot: [`../architecture.md`](../architecture.md).

## Contents
- `intent/` — original problem framing and design intent (old stack).
- `pipeline/` — the 9-step FE pipeline deep-dive (old stack).
- `issues/` — old-stack bug log.
- `INTENT.md`, `ISSUES.md`, `README_PIPELINE.md` — old-stack top-level overviews.

The Korean `ROADMAP.md` that these once accompanied was superseded by the English
[`../roadmap.md`](../roadmap.md); its prior content lives in git history.
