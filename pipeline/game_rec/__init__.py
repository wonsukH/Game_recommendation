"""game_rec — Steam game recommendation agent package.

Submodules:
- io: file I/O helpers for embeddings, sparse matrices, JSON metadata.
- config: YAML config loader (config/default.yaml).
- log: structured logger factory.
- prompts: load LLM prompt templates from prompts/*.txt.

Future submodules (added in subsequent commits):
- data, models, index, evaluation, agent.
"""
