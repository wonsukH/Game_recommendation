# SERVING requirements — what the deployed app needs (Streamlit Community
# Cloud installs THIS file from the repo root). Research/crawl/eval extras
# live in requirements-dev.txt (`pip install -r requirements-dev.txt` locally).

# --- Core data + ML ---------------------------------------------------
pandas==2.3.1
numpy==2.3.2
scipy==1.16.1
scikit-learn==1.7.1

# --- LLM / Agent (Gemini + LangGraph) --------------------------------
langgraph==0.6.6
langchain-core==0.3.74
langchain-google-genai>=2,<3
python-dotenv==1.1.1

# --- Serving I/O ------------------------------------------------------
streamlit==1.48.1
requests==2.32.4
huggingface_hub<1.0            # boot-time EASE artifact download (public repo)
